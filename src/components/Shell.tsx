import Link from "next/link";
export function Shell({ children, eyebrow = "Income Tax e-Filing" }: { children: React.ReactNode; eyebrow?: string }) { return <><header className="border-b border-line bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-5"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-ink text-sm font-bold text-white">IT</div><div><p className="text-sm font-semibold text-ink">{eyebrow}</p><p className="text-xs text-slate-500">Assessment Year 2026–27</p></div></div><div className="flex items-center gap-3"><Link href="/" className="rounded-lg px-3 py-2 text-sm font-semibold text-slate-600 hover:bg-slate-100 hover:text-teal focus-ring">Log out</Link><span className="rounded-full bg-teal/10 px-3 py-1 text-xs font-semibold text-teal">Prototype</span></div></div></header><main>{children}</main><footer className="mx-auto max-w-6xl px-6 py-10 text-xs text-slate-500">This prototype uses synthetic data only. No real tax filing is submitted.</footer></>; }
