import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";
export async function POST(req:Request,{params}:{params:{id:string}}){const b=await req.json();if(!["aadhaar-otp","net-banking","evc"].includes(b.method)||!/^[0-9]{6}$/.test(b.otp)||b.otp!=="246810")return NextResponse.json({error:"Enter the six-digit demo OTP shown on screen."},{status:400});const stamp=Date.now().toString().slice(-10);return NextResponse.json(await prisma.return.update({where:{id:params.id},data:{status:"filed",acknowledgementNumber:`ACK2026${stamp}`,verificationMethod:b.method,verificationDate:new Date()}}))}
